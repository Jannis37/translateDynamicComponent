import { AppPanelDirective } from './app-panel.directive';

describe('AppPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new AppPanelDirective();
    expect(directive).toBeTruthy();
  });
});
